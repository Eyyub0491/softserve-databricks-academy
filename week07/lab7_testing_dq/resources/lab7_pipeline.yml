resources:
  pipelines:
    lab7_pipeline:
      name: lab7_pipeline
      catalog: ${var.catalog}
      target: ${var.silver_schema}

      configuration:
        catalog: ${var.catalog}
        bronze_schema: ${var.bronze_schema}

      libraries:
        - file:
            path: ../lakeflow/lab7_pipeline.py