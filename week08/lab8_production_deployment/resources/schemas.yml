resources:
  schemas:
    bronze_schema:
      name: ${var.bronze_schema}
      catalog_name: ${var.catalog}
      comment: Lab 8 Bronze layer schema.

    silver_schema:
      name: ${var.silver_schema}
      catalog_name: ${var.catalog}
      comment: Lab 8 Silver layer schema.

    gold_schema:
      name: ${var.gold_schema}
      catalog_name: ${var.catalog}
      comment: Lab 8 Gold layer schema.
